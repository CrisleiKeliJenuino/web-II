# API MVC - versão com bugs

Esta API foi preparada para uma atividade prática de debugging.

Objetivo esperado ao final:
- GET /usuarios deve responder JSON com a mensagem da rota de usuários
- GET /hello deve responder JSON com a mensagem da rota hello

Dica:
- Rode a aplicação
- Observe os erros no terminal
- Corrija os arquivos até a API funcionar
